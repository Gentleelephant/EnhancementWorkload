apiVersion: v1
kind: Service
metadata:
  name: {{ include "enhancementWorkload.fullname" . }}
  namespace: {{ include "enhancementWorkload.namespace" . }}
  labels:
    {{- include "enhancementWorkload.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "enhancementWorkload.selectorLabels" . | nindent 4 }}
